{
  "name": "@servops/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain, SQLite schema, repos, conflict policy, automation helpers (Milestones 0–11).",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test test/*.cjs"
  },
  "devDependencies": {
    "sql.js": "^1.11.0"
  }
}
